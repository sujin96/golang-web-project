<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <title>Index</title>
    <link rel="stylesheet" href="/web/static/css/style.css">
     {{/* <script>
         if ( '{{ .Name }}' == '콜라' ) { alert('관리자') } 
         else {const target = document.getElementById('test1');
        target.style.display = 'block'; alert('일반사용자') }  //!  
  </script> */}}
<script type="text/javascript">

         window.onload = function(){
           var refButton = document.getElementById("btnButton"); //! 이유는 모르지만 쓰지 않는 변수안에 집어 넣으니 동작함
           if ( '{{ .Name }}' == '콜라' )
           {
        alert('관리자 계정');
           }
           else
           {
               document.getElementById("btnButton").style = "display:none";
           }
            {{/* refButton.onclick = function() { */}}
                {{/* alert('I am clicked!'); */}}
                
            {{/* } */}}
        };
          </script>
</head>



<body>

          {{/* <script>
         if ( '{{ .Name }}' == '콜라' ) {document.getElementById("target").disabled=true;} 
         else {document.getElementById("target").disabled=true;}  //!  
  </script> */}}
    <div class="main_title">
        <h2>테크노베이션 게시파안</h2>
<a href="dashboard.html">1</a> 
    </div>
    <div class="body_center">

    <span>
        {{/* <button type="button" class="button1" onclick="location.href='/write'">글쓰기</button> */}}
        {{/* <span id="target"> */}}
        
        {{/* <button type="button" class="button1" id="target" onclick="location.href='/board'">랭킹 보드 관리자만 클릭222</button> */}}
        
        {{/* </span> */}}
        {{/* </span> */}}
            <form id="form1">
    <div>
        {{/* <input id="btnButton" type="button" value="Click me"/> */}}
        <button type="button" id="btnButton"class="button1" onclick="location.href='/board'">관리자 전용</button>
    </div>
    </form>

      <a href ="web/templates/dashboard.html">메인페이지 이동</a>
    </div>
            <div class="user_profile">
            <h4> {{.Name}} 님</h4>
            <h4><a href="/logout">로그아웃2</a></h4>
               <h4><p> -내ID : {{.Id}}</p></h4>
    {{/* <h4><p> -백카운마지막날짜 : {{.Created}}</p></h4> */}}
       {{range .id}}
       
            {{.ID}} | {{.Author}} |
            <br><br>
        </div>
    {{ end }}



















    
            <a href="web/templates/dashboard.html">main</a>
        </div>
    </div>
</body>
</html>